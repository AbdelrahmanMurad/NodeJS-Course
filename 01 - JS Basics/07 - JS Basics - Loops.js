const arr = ['BMW', 'MB', 'Nisan'];

const len = arr.length;
for (i = 0; i < len; i++) {
    console.log(arr[i]);
}
console.log('--------------------------------');
arr.forEach(element => {
    console.log(element);
}
)